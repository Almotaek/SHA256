#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* textToBinary(char* string,char* hash);
char* SHA256(char* input,char* hash);
void cpy(char* ,char* );
char* append_f (char *input);
int len(char*);

#endif
